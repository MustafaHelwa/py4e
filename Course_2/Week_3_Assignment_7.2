# Use the file name mbox-short.txt as the file name
fname = input("Enter file name: ")
fh = open(fname)
count = 0
tot = 0

for line in fh:
    if not line.startswith("X-DSPAM-Confidence:"):
        continue

    count = count + 1
    p = line.find(":") + 1
    value = float(line[p:].strip())
    
    tot = tot + value
    
avgg = tot / count

print("Average spam confidence:", avgg)
